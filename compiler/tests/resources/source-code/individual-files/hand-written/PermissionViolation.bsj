import java.io.*;

public class PermissionViolation
{
	[:
		CompilationUnitNode node = getContext().getAnchor().getNearestAncestorOfType(CompilationUnitNode.class);
		ImportListNode importsNode = node.getImports();
		importsNode.clear();
	:]
}