public class Loaded
{
	[:
		#target a;
		context.getAnchor().getNearestAncestorOfType(ClassBodyNode.class).getMembers().add(
				context.getFactory().makeFieldDeclarationNode(
						context.getFactory().makeFieldModifiersNode(
								AccessModifier.PUBLIC,
								true,
								false,
								false,
								false,
								context.getFactory().makeMetaAnnotationListNode(),
								context.getFactory().makeAnnotationListNode()
								),
						context.getFactory().makeVariableDeclaratorListNode(
								context.getFactory().makeVariableDeclaratorNode(
										context.getFactory().makePrimitiveTypeNode(PrimitiveType.INT),
										context.getFactory().makeIdentifierNode("x"),
										null)),
						null));
	:]
}