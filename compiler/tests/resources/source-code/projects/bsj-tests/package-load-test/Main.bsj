[:
	#target a;
	context.getCompilationUnitLoader().load(context.getAnchor().getRootPackage(), "Loaded");
:]

public class Main
{
	public static void main(String[] arg)
	{
		System.out.println(Loaded.x);
	}
}