package mypackage;
#import edu.jhu.cs.bsj.stdlib.metaannotations.*;

import java.util.HashMap;
import java.util.List;

public class DelegateClass {
	
	//@@Delegate(method="Good")
	private Test member;

	
	@@Delegate(method="length", as="newLength")
	private java.lang.String s;

	//@@Delegate(methodName="put", delegateMethodName="addChildren")
	private HashMap<Test,Test> children1;
	
	@@Delegate(method="add", as="addChildren")
	//@@Delegate(method="size", as="mySize")
	
	private List<Test> children;
	
	public DelegateClass(Test o) {
		member = o;
	}
	//@@Delegate(methodName="sayGoodbye",delegateMethodName="add")
	private Test getMember() {
		return member;
	}

	public static void main(String[] args) {
		DelegateClass foo = new DelegateClass(new Test());
		//System.out.println(foo.hiThere(3));
	}
	/*
	public static class Test {
		public String sayHello(int i) {
			return "hello";
		}
		public String sayGoodbye() {
			return "goodbye";
		}
	}
	*/
}